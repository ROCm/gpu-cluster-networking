name: Linting

on:
  push:
    branches:
    - develop
    - 'docs/*'
    - 'roc**'
    - staging
  pull_request:
    branches:
    - develop
    - 'docs/*'
    - 'roc**'
    - staging

jobs:
  call-workflow-passing-data:
    name: Documentation
    uses: ROCm/rocm-docs-core/.github/workflows/linting.yml@develop

